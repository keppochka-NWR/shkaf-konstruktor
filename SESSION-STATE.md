# Состояние сессии — 03.09.2026 (перед компактизацией)

Файл-передача контекста: здесь всё, что нужно, чтобы продолжить без истории чата.

## Конструктор шкафов — ОСНОВНОЙ проект

**Код:** эта папка, локальный git (последний d364371). Запуск: `ЗАПУСК.bat` → uvicorn server/app.py на 8102 (статика + облако). Дев-конфиг `konstruktor` в launch.json — тоже uvicorn.

**Добавлено после первого SESSION-STATE (фидбэк Max):** клик-размеры починены (кража фокуса mousedown + кэш браузера + Enter-детект; ресурсы версионируются ?v=N, сервер шлёт no-cache); холст во весь экран (fitViewBox под аспект окна + бесконечные фоны во всех видах); **облако**: server/app.py (FastAPI+SQLite: аккаунты по коду на почту DEV-режим, CRUD проектов, .env для SMTP на проде), web/assets/cloud.js, блок «Облако» в свойствах проекта (вход, сохранение, список, автосейв с дебаунсом 4с при наличии cloudId). Проверено e2e: вход max@test.ru, демо сохранено/загружено из облака.

**Что работает (проверено живьём):** тетрис модулей (нижние на цоколе 80 / верхние), наполнение drag&drop (полки, штанга, секции ящиков count×boxH, сетка Леманы), фальш = панель 16 мм только в зоне выкатного элемента по сторонам петель, автоподгонка модуля под техничку сетки, петли по стандарту цеха, 133 декора Lamarty кликом, виды Спереди/Сбоку/Сверху, глубина у модуля своя (mod.depth), клик по жёлтому размеру = инлайн-ввод, зум колесом/пан ПКМ/dblclick сброс, Ctrl+Z, Delete, шаблоны (платяной/с ящиками/полочный), дублирование, библиотека проектов в localStorage (там демо «Прихожая (демо)» на 100 400 ₽), смета (закупка×наценка, наценка в свойствах проекта), раскрой (гильотина, КИМ, карты листов), КП (окно печати или скачивание HTML), живой чертёж демо на лендинге.

**Архитектура:** constants.js (конструктив цеха из vault), catalog-lamarty.js + tex/ (129 jpg), hardware.js (направляющие с вычетами, петли, штанги), lemana.js (16 ручек + 10 сеток, артикулы настоящие, спарсено 01.09), model.js (проект/модули/деталировка/edgeMeters/библиотека), rules.js (canInsert/warnings), nesting.js (раскрой), pricing.js (priceProject), render.js (3 вида, data-dim для кликабельных размеров), quote.js (КП), editor.js (UI), editor.css. Миграция старых проектов: migrateProject().

**Очередь (Max: «шлифовать до готовности к продаже»):** перегородки внутри модуля (модель секций готова частично), угловые модули (вид сверху готов под них), редактор купе (двери из Calc/kupe: полотно = проём−40, ширина = (проём+(N−1)×22)/N), точный нестинг под ЧПУ, сборочные чертежи (наработки b3d-tools), УП (nesting-gcode) и отправка на почту цеха, аккаунты/облако вместо localStorage.

**Согласовать с Max:** лимиты модулей (его ТЗ 1200×2200 vs СТП цеха 900×2000 — заложено 1200×2200); вычеты скрытых направляющих (21/сторона TODO) и метабоксов (17 TODO); пролёт полки 800; drawerStep 40 (просвет в стопке ящиков).

## Просчитано (агрегатор)

MVP полный, локально: `Claude Project\Агрегатор\mvp` (git main, 9 тестов зелёные). Кухня+шкаф анкеты, кабинет, админка, архив цен, выбор цеха, десктоп-вёрстка с кухнями. Запуск ЗАПУСК.bat → 8100. Ждёт от Max: VPS (деплой по ДЕПЛОЙ.md), домен, SMTP-ящик, TG-бот. Лендинг опубликован: keppochka-nwr.github.io/proschitano-landing (репо proschitano-landing).

## Прочие хвосты сессии

- SIGame: явный запрос «проанализируй как делают HTML для свояка» — не сделан.
- CRM-миграция: ждёт от Max списка скриптов боевой таблицы (анализ в CRM\PROD_MIGRATION_ANALYSIS_2026-08-29.md).
- Цех-бот: план в tg-bot\план.md, ждёт аренды VPS (порт бота 8200 рядом с Просчитано 8100).
- Беседка: живая (Pages), заглушки телефона.
- Дев-порты: calc 8742, besedka 8743, sigame 8744, agregator 8745, proschitano 8100, konstruktor 8102, проверочный 8101 (временный, глушить).
