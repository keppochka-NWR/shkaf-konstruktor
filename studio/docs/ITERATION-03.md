# Ночная итерация 03 · 6 сентября 2026

## Основание
Последние прямые указания Max имеют приоритет над старым СТП: отдельные корпуса до 900 × 2200 мм, Lamarty 2750 × 1830. Источники и бизнес-цель записаны в WORKSHOP-RULES.md. Старые значения 1200 мм / дно HDF / ширина ящика не больше направляющей отменены.

## Рабочая реализация
- Перетаскивание корпусов: в 3D и сверху по полу, спереди по ширине/высоте; привязки к соседям и стенам, запрет пересечений. Антресоль сверху без цоколя.
- Перетаскивание полок/ящиков/штанги внутри корпуса; добавление наполнения перетаскиванием из библиотеки. На отпускании проверяются ограничения, невалидное действие откатывается. Undo/redo.
- Дно всех ящиков ЛДСП16; отдельные фасады и ручки, обязательная полка над группой, фальшпанели на сторонах петель. Внутренний ящик утоплен для ручки.
- Фасады новых модулей включены; широкому проёму две створки. Сторона одиночных петель настраивается.
- Задняя стенка набивная или в паз. Параметры паза редактируемые, пока требуют технологического подтверждения; четвертей нет.
- Штанги с фланцами; условная геометрия пантографа по диапазонам найденных GTV в базе (не точная модель производителя).
- Помещение с четырьмя стенами, окнами и дверями: сторона, отступ, ширина/высота/подоконник. Геометрия стен вырезает настоящие проёмы; пересечения проёмов запрещены.
- Новая компактная библиотека и панель действий, нейтральная сцена/синий акцент.
- Формат проекта v3 и localStorage module-studio-v3. Старые v1/v2 ключи сохранены; старый корпус >900 разбивается на независимые корпуса, общая ширина сохраняется.

## Проверено
29 тестов проходят: правила цеха, геометрия, фасады/фальшпанели/дно, короткие направляющие в широком ящике, паз, перемещение ящиков, вставка наполнения, стыковка/антресоль/коллизии, миграция, стены/проёмы, карты листов и экранирование КП. Сборка TypeScript/Vite успешна.
Ручная проверка CUA: перемещение корпуса меняет координаты, выход за помещение отклоняется; полка перетаскивается с пересчётом двух зазоров; новая полка перетаскивается из библиотеки; окно добавляется с параметрами; тестовые изменения отменены. Оригинальная открытая композиция перенесена в 3 корпуса 500+500+400 мм. Старые ключи сохранения не удалялись.

## Продолжить ночью, цель активна до 10:00 МСК
1. Ещё проверить чувствительность перемещения в перспективе, прозрачный пустой корпус, перетаскивание ящика и пантографа, размеры антресоли. Пантограф/штанга должны перемещаться вместе с фланцами/рычагами. Сейчас род-фланцы при preview могут не двигаться до отпускания.
2. Улучшить дизайн дальше, в том числе план помещения, добавление модулей вдоль разных стен/поворот и визуальное выделение перетаскиваемого объекта. Сейчас ориентация корпуса одна.
3. Панель высоты штанги сейчас показывает 0.85h если явного rodAt нет — исправить на фактическую геометрию. Групповой автоколпак над разнесёнными ящиками один, полки ниже группы не допускаются — расширить модель интервалов.
4. Удаление выбранной полки/ящика и перенос наполнения между корпусами, выбор/открытие выкатного ящика, петли в 3D. Перенос между корпусами пока не реализован.
5. Изучить остаток бизнес-цен/замеров и восстановить смету старого редактора. Прайс Vault: лист Лемана 30000, шариковые1000, скрытые1500, пантограф9000, корзина6000, подсветка3000/4000п.м., узкие детали<70 +300. Это отдельный профиль; не смешивать с закупка×2.2.
6. Реальные серверные кабинеты и админ: старый server/app.py существует, но в студию не подключён. Перед тестами отделить БД через конфигурацию, не трогать живой data.db; DEV возвращает код, не выдавать его за безопасный прод. Сервер8103 сейчас только статика. Внешний деплой и SMTP не настроены этой итерацией, письма не отправлять без авторизации.
7. Улучшить фото/рендер, документацию/бирки/чертежи. УП пока не выдавать как готовые: отсутствуют проверенные постпроцессор/присадка/пазы/инструменты.

Heartbeat `automation` раз в30мин до 2026-09-06T07:00Z настроен в этой задаче; не дублировать. Goal активен, не завершать преждевременно. Новые задачи/подагенты не нужны. Локальный git codex/module-studio.

## Дополнение после 7124d9a
- Удаление выбранной полки/ящика/штанги/пантографа через кнопку и Delete. Остальные ящики сохраняют абсолютную высоту. Ctrl+Z восстанавливает. Ручная проверка удаления средней полки и undo прошла.
- Превью перетаскивания штанги включает фланцы, пантографа — рычаги; поле высоты берётся из реальной геометрии.
- Добавлена Смета во вкладках документов. Закупка из локальной базы + кромка/фурнитура/работа × коэффициент2.2. Листы из nest(project), недостающие цены явно требуют ввода; никакого молчаливого defaultSheet. Цена КП переносится отдельной кнопкой, автоматически не меняется.
- Покрытие: 33 теста проходят, TypeScript/Vite. В браузере проверена смета текущего проекта: GTV0FPO550 без цены → итог недоступен; ввод тестовой1200 → пересчёт. Тестовые цены полностью отменены двумя undo (вторая запись возникла от blur неизменённого поля; это исправлено).
- Источники сметы: web/assets/constants.js (листы/кромка/работа), Vault Закупочные цены поставщиков.md (петля115, ручкаUZ819128=100, GTVball250/350/450/500=445/550/650/695). GTV0FPO и пантограф закупки не найдены, цена не подменяется DTC/Unihopper. Лемана30000/лист и9000пантограф — отдельные розничные цены, пока не подключены. Крепёж без описанных количеств, доставка и монтаж не включены — указано в интерфейсе.
- Следующее: оптимизация раскроя (сейчас простая гильотина может расходовать лишние листы), перенос наполнения между корпусами, поворот модулей вдоль боковых стен, серверные кабинеты, визуализация. Ночная цель остаётся активной.

## Следующее дополнение: раскрой и перенос между корпусами
- Раскрой сравнивает 6 размещений свободными прямоугольниками (3 порядка ×2 критерия) с прежней гильотиной. Поворота деталей нет, направление текстуры сохранено. Промежуток резервируется расширением прямоугольников, за границей листа не добавляется. Итог никогда не использует больше листов прежней схемы на том же проекте.
- 38 тестов проходят. 40 случайных наборов ×3 промежутка проверяют границы/пересечения/направление/уникальность; нагрузочные проекты1/3/10/40 корпусов. 40 одинаковых корпусов:75→73 листа. Это эвристика для технолога, не глобально оптимальный нестинг или готовые УП.
- Перенос полки/ящика/штанги/пантографа между корпусами или секциями отпусканием на целевом корпусе. Направляющая и высота ящика сохраняются. При непомещении операция целиком отклоняется, источник не изменяется. Ручной перенос полки1→3 и undo проверены. Превью пока показывает только вертикальное смещение исходного элемента, горизонтальный переход происходит на отпускании — улучшить.
- Исправлена текстура фасадов ящиков: теперь используется part.decor, а не материал корпуса.

## Поворот корпусов
- У размещённого корпуса rotation=0/90/180/270. x/z — минимальный угол прямоугольника корпуса без выступов; bounds учитывает повёрнутые фасады/заднюю стенку. localToRoom/roomToLocal — общий преобразователь для выбора секции, привязок и геометрии.
- 3D-группа имеет собственное положение/поворот, детали хранят локальные координаты. Размеры/выделение поворачиваются вместе с активным корпусом. Вид спереди/сбоку относительно выбранного корпуса. Антресоль наследует глубину и поворот.
- 40 тестов проходят. Браузер: третий корпус перемещён наx1800, повёрнут90°, фронт переключён и полка перемещена; зазоры пересчитались. Все3 тестовые правки отменены, x1050/rotation0 восстановлены.
- Следующее крупное улучшение: наглядный план помещения с перемещением корпусов/проёмов; кабинеты на сервере; режим презентации/изображение без размеров. Проверенные производственные УП ещё не готовы.

## Интерактивный план помещения
- Новый вид План и переход из Замер помещения. SVG-план с сеткой, габаритами комнаты, ориентацией фасада, пунктиром верхних модулей и оконными/дверными проёмами.
- Корпуса перетаскиваются в плане с общими snapPlacement/projectErrors; окна/двери перемещаются вдоль своей стены с шагом10мм. Правки коммитятся на отпускании, отменяются Ctrl+Z.
- В браузере корпус смещён сz30 до904 и отменён; окно добавлено и перетащено сoffset0 до1570, обе правки отменены. Исходная композиция восстановлена. Сборка успешна, правила опираются на ранее проверенное ядро40тестов.
- План скрывает неработающие в нём инструменты3D. Открытие документов из плана возвращает3D с помещением, чтобы КП могло получить изображение. Перетаскивание наполнения из библиотеки также возвращает3D.
- Приоритет следующей итерации: режим показа клиенту/экспорт изображения; библиотека проектов и серверный кабинет с правами менеджер/админ. Оставлять ночную цель активной до10МСК или лимита.

## Серверный кабинет
- Подключён CloudPanel через /api/studio, отдельный server/studio_app.py и SQLite studio-data.db. Старая data.db не открывалась этим кодом. Детали запуска/настроек/ограничений: SERVER-CABINET.md.
- 5 серверных тестов безопасности/сохранения проходят во временных БД, 40 тестов редактора и сборка проходят.
- В браузере менеджер вошёл, сохранил «Прихожая — пример проекта», загрузил обратно. Администратор вошёл и увидел этот проект через Все сотрудники. Пример оставлен в локальном кабинете, геометрия исходного проекта не изменялась. Текущий cookie — локальный администратор; это явно отмеченный общий демо-профиль.
- Node сервер8103 перезапущен, актуальный exec session43132, Python API8104 childPID56548 (проверять актуальность перед действиями). Его предшественник PID50972 был остановлен адресно. START-3D теперь стартует API автоматически; Python задаётся STUDIO_PYTHON, defaultruntimeCodex.
- STUDIO_DEMO=1 толькоloopback, реальная почта заблокирована, письма не отправлены. Внешний хостинг/HTTPS/SMTP не настроены. Пользователю это объяснено, не называть текущую версию облачным размещением.
- cloud{id,revision,owner} теперь часть истории проекта и JSON; при сохранении старой revision API отвечает409. Автосохранение пока тольковбраузере, серверное — кнопкой. Историяверсий вБД сохраняется, UI восстановления отдельных ревизий ещё нет.


## Показ клиенту и изображение
- Кнопка «Показать клиенту» скрывает панели, размеры, выделение и сетку; оставляет выбор ракурса, помещение и открывание фасадов. Возврат в редактор сохраняет проект.
- Изображение формируется до2560px по длинной стороне, без рабочих линий, с предпросмотром. После захвата возвращаются размер canvas, pixelRatio, фон и видимость направляющих.
- Предпросмотр в IAB проверен:2560×2299. Прямая ссылка PNG в Edge действительно сохранила Downloads/Проект мебели.png (299058байт, отдельный стартовый пример). В IAB скачивание не подтвердилось, подсказка предлагает Edge/Chrome. Не называть этот вид фотореалистичным рендером: это увеличенное изображение текущей Three-сцены.
- Презентация не добавляет отключённые пользователем фасады; показывает конструкцию проекта. Текущий пользовательский проект не изменён проверкой.


## История серверных проектов
- В кабинете кнопка История показывает последние100сохранений и позволяет открыть выбранную версию. Открытие не меняет сервер; сохранение создаёт новую ревизию поверх актуальной, сохраняя все промежуточные версии. Оптимистическая проверка защищает от изменения проекта другим окном после загрузки истории.
- Доступ к списку и данным ревизии проверяется на сервере: владелец или администратор. Администратор по-прежнему сохраняет чужой проект только своей копией.
- 6серверных тестов проходят, включая недоступность истории чужому менеджеру, чтение админом, восстановление версии1 новойверсией3 с сохранением версии2. Сборка проходит. Браузер: администратор открыл историю сохранённого примера и версию1; показано сообщение, что сервер не изменён. Геометрия примера совпадает с текущей.
- Сервер перезапущен: execsession17879, API PID41556. Проверять процессы перед дальнейшим перезапуском.


## Обратная связь при перетаскивании
- Подсветка объёма целевой секции и подпись Корпус/секция при переносе наполнения, включая добавление из библиотеки. Подсветка показывает адрес назначения; окончательная проверка свободного места остаётся при отпускании.
- Перетаскиваемые детали исключены из поиска цели, чтобы они не перекрывали секцию. В режиме Наполнение нажатие на конструкцию выбирает её, но не запускает перемещение всего корпуса.
- Браузер: средняя полка первого корпуса перенесена в третий, число полок3 и зазоры пересчитались; перенос отменён. Ошибок консоли нет. Сборка проходит. Превью самой переносимой детали пока вертикальное в исходном корпусе, адрес назначения дополнительно виден рамкой.


## Проверочные бирки
- Документы → Бирки деталей: каждая плитная деталь имеет тот же код, что деталировка и карта листа, плюс фактический номер листа, модуль, материал, толщина и габариты.
- HTML для печати90×50мм, две колонкиA4, отметкаПРОВЕРКА и время формирования. Коды порядковые в текущем проекте, поэтому после изменения конструкции нужно формировать комплект заново. Это ещё не производственные бирки с присадкой/штрихкодами/подтверждёнными сторонами кромления.
- 41тестредактора проходит, новыйтест проверяет взаимнооднозначные коды, фактическийлист и экранирование пользовательскихназваний. Предпросмотр в браузере проверен. Печатная пагинация экспортного HTML ещё требует отдельной визуальной проверки.


## Ведомость комплектации
- В Карты листов добавлена Ведомость комплектации HTML: помещение и проёмы, каждый корпус с материалом/положением/поворотом/цоколем/задней стенкой/фасадами, секции с высотами полок, направляющими и высотами каждого ящика; примечания и место проверки технологом.
- 42теста проходят, новыйтест проверяет выбор заднейстенки, окно и сведения о ящиках. Сборка проходит.
- Бирки действительно скачаны в Edge (Downloads/Бирки деталей.html,12313байт для стартового примера). Открытие file URL заблокировано Browser Use URL policy; обходов не делал. Печатная пагинация остаётся непроверенной; пользователь об этом уведомлён.


## Сверка с бланком задания на корпус
- Прочитан локальный Производство/Бланки/ТЗ на корпус.html. Бланк содержит дату/номер замера, цвет кромки, заднюю стенку без/ЛХДФ/ЛДСП, цоколь/опоры, варианты петель/ручек/направляющих.
- Добавлена project.measurement{number,date,notes}, необязательная для старых файлов. В Замер помещения раскрывается Карточка замера, данные сохраняются в JSON/кабинете и ведомости. Валидация запрещает невозможные даты и чрезмерные строки.
- 43теста проходят. Браузер: номерПроверка-123 и дата2026-09-06 введены, значения подтверждены DOM, общая отмена убрала оба пробных значения (сгруппированы одной операцией).
- Следующие конструктивные пробелы по бланку: опоры, задняястенкаЛДСП/беззадней, цветкромки, разныепетли/ручки/открывание. Не добавлять декоративный переключатель без изменения деталей и сметы.


## Ручки и согласованность направляющих
- DrawerConfig.handle?:boolean: по умолчанию у GTV0FPO Push нетручки, у шариковыхесть128мм. ДляPush можно явно добавитьручку. Выбор сохраняется/переносится с ящиком; геометрия и смета получают фактическое количество. Шариковый ящик безручки отклоняется, поскольку другого механизма открывания нет. Ведомость показывает ручку отдельно.
- Подбор длины и подписи вариантов в UI теперь используют rearClear и зазор44мм для внутренних ящиков/25мм для открытого корпуса, как validate. Прежний UI игнорировал паз и предлагал слишком длинную направляющую.
- Ручку ящика теперь можно использовать для перетаскивания самого ящика в режиме Наполнение.
- 44теста проходят: автоматическое отсутствие ручекPush, явнаяручка и импорт. Сборка проходит.


## Масштаб рабочей сцены
- Вписывание теперь рассчитывает проекции восьми углов объёма в выбранном ракурсе и перспективе, вместо суммирования ширины и глубины для всех видов. Учтены повороты выбранного корпуса, помещение, размер окна и запас для размерных подписей.
- На текущем узком окне фронтальная композиция стала примерно380px вместо290px, подпись высоты видна полностью; скриншот проверен после корректировки запаса. Без размеров и в презентации запас меньше.
- 45тестов проходят, новый проверяет границы кадра при aspect0.4/1/2 и отсутствие излишнего уменьшения фронтального вида. Сборка проходит.


## Задняя стенка ЛДСП и открытый корпус
- Варианты из бланка: board — вкладнаяЛДСП16 в цвет корпуса между боковинами/дном/крышей; none — безстенки. Существующие nailed/groove сохранены, четвертей нет.
- Для board свободная глубина уменьшается на17мм (16+1 зазор), у полок/перегородок/фальшей/крышкиящиков пересчитывается глубина, валидатор направляющих учитывает её. Стенка включается в ЛДСП-раскрой/цену, ЛХДФ исключается. Крепёж ещё не рассчитан; UI требует проверки технологом. none также требует проверки жёсткости/крепления.
- 46тестов проходят: boardгеометрия, свободнаяглубина, отсутствиеHDFлистов, roundtripboard/none. Браузер: вкладнаястенка видна вцветкорпуса, noneуменьшаетдетали9→8; обе проверки отменены. Сборка проходит.


## Ошибки размещения во время перетаскивания
- В3D плашка получает ошибку projectErrors(moveModule(...)) и красныйфон доотпускания. На плане предварительныйпроект проходит тотжевалидатор; ошибочный корпус обведёнкрасным, внижнейплашке причина.
- Браузер: корпус1 на плане перетащен поверх2; показано «Изменение не применено…пересекается…», undoосталсяdisabled, положение не сохранено. Промежуточныйкадр во время drag отдельно не снят. Сборка проходит; расчётныеправила неизменны и ранее покрыты46тестами.


## Библиотека настроенных модулей
- Моя библиотека модулей хранит до30 шаблонов в localStorage module-studio-library-v1. Сохраняет размеры/материалы/наполнение, собственное название. Вставка использует appendModule с новыми idсекций и размещённогокорпуса, затем сохраняет название шаблона.
- Это локальная библиотека браузера, не общая серверная; ограничение указано в UI. Чтение проходит parseModule, запись ловит ошибку квоты.
- Браузер: шаблонПроверочныйполочный500 сохранён, вставленчетвёртымкорпусом, имяподтверждено, вставкаотменена; пробныйшаблонудалён. Сборка проходит.


## Размещение новых модулей и копий
- appendModule принимает исходный размещённыйкорпус длякопирования: сохраняются rotation/y, новыеidкакраньше. Новые шаблоны ищут место у соседей/границкомнаты; если первыйряд заполнен, рассматриваются другие ряды.
- Перебор использует реальные bounds и overlap, не поворачивает корпус самовольно. Если места нет, общаявалидация отклоняет вставку.
- 47тестов проходят, новый проверяет rotation90, уникальностьсекций, неизменностьисточника и вставку следующего корпуса в другойряд в комнате1500мм. Сборка проходит.


## Встроенный рабочий маршрут
- Как пользоваться переписано подактуальныережимы:6кнопок замер/корпуса/наполнение/документы/показ/кабинет открывают нужныйраздел. Уточнены режимкамеры, перетаскивание, CtrlZ/Delete/Escape, библиотекашаблонов и границыпроизводственногоэкспорта.
- README обновлён: удалены устаревшие пределы1200мм/20корпусов и утверждения об отсутствии окон/поворота/кабинета.
- Браузер: шаг1открыл интерактивныйплан, наличие плана подтверждено. Сборка проходит.


## Вычеты ниши по СТП
- Повторно прочитан исходный Стандарты производства..md, раздел8. Добавлен measurement.niche с минимальными шириной/высотой/глубиной и отклонением стены; вычисляемые пределы не хранятся.
- Ширина−10мм при отклонении<10, иначе−15мм; высота−30; глубина−5. В СТП точное равенство10 не описано, принято15и явно указано вUI. Пользовательскийпределвысоты2200 сохраняется поверх старогоСТП2000.
- Карточка и ведомость показывают пределы; геометрия комнаты/модулей автоматически не меняется. 48тестов проходят, включая формулы/roundtrip/независимостьразмеровкомнаты. Браузер:3990×2670×2995 по текущимкомнатнымначальнымзначениям, пробное включение отменено. Сборка проходит.


## Материал фасадов ящиков
- Module.drawerFacadeDecor необязателен; безнего наследуется facadeDecor распашныхфасадов. Выборотдельно в свойствахмодуля, сброс«Как у распашных фасадов». Геометрия, раскрой, бирки/смета черезparts используют выбранныйдекор; ведомость указываетего. Импортпроверяеткаталог.
- Исправлен preset: готовое наполнение теперь копирует текущийкорпус и заменяет толькоsections, сохраняя заднююстенку/цоколь/фасады/материалы вместо сбросакinitialModule.
- 49тестов проходят, новый проверяет разные материалыдверей/ящиков, roundtripиfallback. Браузер: во2корпусе выбранГрафитдляящиков, подписьподтверждена, правкаотменена. Сборка проходит.


## Соответствие КП комплектации
- В КП материалы подписаны отдельно: корпус/двери/фасадыящиков. Полки считаются по role=shelf изparts, включая обязательныенадящиками; пантографыуказаныотдельно.
- 50тестов проходят. Новыйтест проверяет отдельныйГрафитящиков, Белыйдверей и2полки(съёмная+обязательная)встартовоммодуле. Сборка проходит.
